services:
  - type: web
    name: interviewcoach-api
    env: python
    plan: free
    branch: main
    autoDeploy: true
    rootDir: Server
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
